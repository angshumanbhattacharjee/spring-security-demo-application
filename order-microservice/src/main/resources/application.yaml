spring:
  application:
    name: order-microservice

  datasource:
    username: root
    password: rootrootroot

eureka:
  instance:
    instance-id: ${spring.application.name}:${random.uuid}

server:
  port: 0

